# 2026-09-22

## 实施 ADR-0016：借鉴 OCR 委托模式增强 review_changes

- 按 ADR-0016 实现顺序完成全部 6 步，测试 35 passed（含新增 13 个），linter 干净。
- ①密钥门：`change_analysis.py` 新增 `_is_secret_path` 纯函数（OCR 10 条 glob + `.env` 家族 + `*.pem/*.key/credentials.json/secrets.yaml`，`.env.example/.sample/.template` 豁免，大小写不敏感）+ `_binary_paths_from_diff`；`collect_git_changes` 返回新增 `excluded: [{path, reason}]`，密钥/二进制文件不进 changes/untracked。单测 `tests/test_secret_path_gate.py` 12 个。
- ②评审门：`review_changes.py` 新增 `_review_gate`（vendor/node_modules/dist 等噪音目录、`.min.js/.lock/.snap` 等后缀、单文件 2000 行体量上限），prepare 时应用；excluded + excluded_summary（分类计数）装配进 prepare 输出。
- ③覆盖率软警告：`_handle_submit` 新增 `_coverage_warnings`——读上次 prepare 的 review_context.json，changed_sources 中既无 finding 也无 skipped+reason 的文件列入 warnings（软警告不 reject；skipped 无 reason 不算覆盖）。
- ④清单排除项：zh/en catalog 给 performance/code-quality/py-mutable-default 加 `exclusions` 文案（OCR 验证过的三条：热路径确认、.pyi 存根豁免、只读默认值豁免）；`builtin_checklists` 装配时用 `i18n.MISSING_PREFIX` 哨兵判断缺失 key（缺失不带字段，向后兼容）。项目覆盖层 YAML 天然支持（条目 dict 原样传递）。
- ⑤prompt 增强：`prompts.py` 步骤 5 加「评审纪律（强制）」段——覆盖率去向、别停在第一个 blocker、行号锚定（>> 前缀行号空间）、excluded 文件不读内容、secret 排除提醒用户。
- 踩坑：i18n.t 缺失 key 返回 `[missing-i18n-key]` 哨兵而非 None，判断字段存在必须用 MISSING_PREFIX 前缀匹配。

## 狗粮评审：用 ADR-0016 增强版流程评审 ADR-0016 实施本身

- 走完 analyze_changes → review_changes(prepare) → 人工四轴评审 → submit 全流程，报告落盘 `.codewiki/workspace/review_reports/20260922-123429-*.json`。
- **覆盖率软警告当场抓到偷懒**：submit 返回 `coverage: 3/5 changed files have no finding and no skip reason`（prompts.py、review_checklist.py、test_secret_path_gate.py 审了没给去向）——ADR-0016 决策 5 的机制有效。
- 发现并修复 2 个 major：
  - **f1（本次实施引入）**：`collect_git_changes` 里 `_ext` 扩展名过滤在密钥/二进制门**之前**执行——.env（无后缀）、.pem、.png 不在 `_SRC_EXTS`，先被静默丢弃，excluded 数组永远收不到 secret/binary 记录，「逐文件标注排除原因」对最典型的密钥/二进制文件失效（本次评审 excluded 为空即佐证）。修复：门顺序改为 secret → binary → _ext，untracked 分支同理（顺带避免对密钥文件 read_text）。
  - **f2（存量 bug 被狗粮暴露）**：`_build_changed_sources` untracked 分支 `by_file[file] = header + body` 赋值字符串，返回值 `"\n\n".join(blocks)` 对字符串逐字符 join——untracked 新文件在 changed_sources 里渲染成单字符流。修复：改 `setdefault(rel, []).append(...)`。
- 新增回归测试：`TestGateOrdering`（真实 git 仓库验证 .env/.pem 进 excluded、a.py 进 changes）+ `TestUntrackedRendering`（源码断言）。35 passed。
- 1 个 minor（f3 coverage 范围错位）按 ADR 暂接受。
- 经验：**新功能落地后立即用自己评审自己**——两个 major 都是纯靠「覆盖率强制 + excluded 应非空而实为空」的交叉核对发现的，单看 diff 看不出来。

## 用户纠正：评审后不得自动修复，须先展示报告并获批准

- 狗粮评审发现 2 个 major 后我直接修复了，用户指出流程错误：`变更评估与代码评审` 命令只授权产出报告，修复是独立决策，必须先展示完整评审报告 + 修复方案，用户同意后才能动手。
- 这正是我们从 OCR 借鉴的原则（SKILL.md Step 7："If the user requested 'review and fix'"——修复是 opt-in），我研究时强调过、执行时违反了。
- **约定（长期有效）**：评审类命令（review_changes、委托模式评审等）产出 findings 后停在报告展示；「review and fix」必须用户显式说出才进入修复阶段。
- 本次两个 major 修复已写入工作区未 commit，等用户选择保留/回滚。

## OCR delegate 模式评审变更区（open-code-review-delegate skill）

- 用 `ocr delegate preview` 圈定 6 个可评审文件（8 个被排除：md/jsonl 不支持扩展名、tests/ 走 default_path），workspace 模式，+720/-19。
- 评审结论：f1/f2 两个 major 已在狗粮评审中修复（工作区代码已是修复后版本，门序 secret→binary→_ext 正确）；21 个测试全过。
- 遗留发现（本次报告）：
  - medium：`_review_gate` 的 oversized 判定会把超 2000 行的 untracked 新文件整体排除，无「截断保留」选项——大文件评审盲区。
  - medium：`_coverage_warnings` 读的是 `workspace.root/review_context.json`，跨仓库/多 prepare 会话下可能读到陈旧上下文（无时间戳/仓库校验）。
  - low：`registry.py` 的 review_changes inputSchema 未声明 `skipped` 字段，调用方只能靠 prompt 约定。
  - low：`tests/test_review_changes.py` 的 `main()` 漏注册 `test_submit_coverage_warnings`（pytest 可发现，裸跑 main 会漏）。
  - low：`_SECRET_DIRS`/`_NOISE_DIRS` 用 `f"/{d}" in p` 匹配中间目录，`a/.ssh/x` 类路径靠前缀兜底，深层相对路径语义 OK 但依赖 `_norm` 先归一化。

## 评审遗留 L1/L2 顺手修复

- L1：`registry.py` review_changes 的 report 描述补上 `skipped: [{file, reason}]`（reason 必填才算覆盖），与 `_coverage_warnings` 消费端和 prompt 约定对齐。
- L2：`tests/test_review_changes.py` 的 `main()` 补注册 `test_submit_coverage_warnings`，裸跑不再漏测。
- 验证：pytest 7 passed（含 coverage 测试）。

## 护栏实施：防止评审后未经批准自动修复

- 用户问"需要优化代码或提示词吗"，确认实施后落地双保险：
  - `prompts.py` change-review 评审纪律新增「修复边界」条款：评审命令只产出报告，findings 展示后停下，用户明确说"修复"才动手。
  - `review_changes.py` `_handle_submit` 返回新增 `fix_gate` 字段，submit 时提醒宿主 Agent 报告只读、修复需用户批准。
  - `tests/test_review_changes.py` 补 `fix_gate` 断言（检查"不得自动修复"字样）。
- 验证：pytest 21 passed（test_review_changes + test_secret_path_gate），linter 干净。
- 教训已落草稿笔记 `repowiki/notes/2026-09-22-评审命令只产出报告修复必须经用户明确批准.md`（lesson, draft, severity=high），待用户 confirm_note。冲突检测返回的 2 条相似笔记均无关（lint 备份、cognee 调研），无需退役。

## 提交推送

- 全部变更（ADR-0016 实施 + f1/f2 修复 + 护栏 + 测试 + ADR 文档 + 文章 + 笔记）以 `941f5a6` 提交并推送 `origin/develop`（0db1634..941f5a6，17 文件 +866/-22）。
