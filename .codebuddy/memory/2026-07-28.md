# 2026-07-28

## CodeWiki LLM Wiki 生成完成
- 为 CodeWiki-CN 仓库生成了完整 LLM Wiki，位于 `repowiki/`。
- 共 **26 个模块文档**（528 组件，6 个顶级模块：CLI / DependencyAnalyzer / Frontend / LLM_Backend / MCP_Server / SharedConfig）。
- 本次补全剩余 5 个 MCP 文档：MCP_Tools_Dependency / MCP_Tools_DocWriter / MCP_Tools_Knowledge / MCP_Tools_Quality / MCP_Server(概览)，均用 code-explorer 子代理读源码返回草稿、主代理落盘。
- 重建了 `repowiki/wiki/index.md`（26 模块索引）、`repowiki/wiki/overview.md`（架构总览 + mermaid）、`repowiki/wiki/log.md`。
- 一致性校验：`.meta/module_tree.json` 与 `.codewiki/workspace/mod_comps.json` 组件集合完全一致（528，无 stale/缺失），文档组件 ID 均取自该源。
- 注意：`schema.yaml` 的 `cross_reference_format` 为 `[[{module_name}]]({{module_name}}.md)`，但实际文档统一使用 `[[ModuleName]]` 形式（与前序 session 及 AGENTS.md 链接一致）。lint 若报格式不符，属既有约定差异，勿改文档以迁就 schema。

## lint_wiki 收尾校验（用户要求）
- 运行 `lint_wiki` 全员检查（checks=all, severity=info）：**total_issues=0，health_score=100**。
- 全部 12 项通过：broken_links / stale_refs / undocumented / cycles / coverage / orphan_pages / no_outlinks / missing_aliases / stale_sources / superseded_pages / overview_stale / unsupported_claims。
- 结论：26 文档与代码完全一致，Wiki 生成任务正式收尾。

## close_session 漏调用根因 + MCP server 代码改动
- 根因：CodeWiki 的 Wiki 生成本就不是原子操作，没有"一键生成并收尾"工具；`close_session` 需 Agent 显式末步调用。本次因多轮/子代理拆分，主代理写完文档 + 跑 lint 后误以为"已等价完成"，漏掉 close_session，直到用户追问才补。
- **关键事实**：`_prompt_generate_wiki` 步骤 6 与 `close_session` 工具描述**原本就已写明要调用 close_session**——所以"补写提示"无效；真正失效点是提醒位置：Agent 收尾停在 `write_doc_file`/`lint_wiki`，从未读到 close_session 描述。
- 改动 `codewiki/mcp/server.py`：(1) `write_doc_file` 描述末尾加 MANDATORY FINAL STEP 强制 close_session；(2) `lint_wiki` 描述末尾同样加；(3) `_prompt_generate_wiki` 步骤 6 改为"强制，不可跳过"并说明漏掉后果。将提醒放到 Agent 最后实际调用的工具描述才能防遗漏。需重启 MCP server 生效。
- 教训：要防止某收尾动作被漏，应在 Agent 收尾时**最后接触的工具描述**里放强制提醒，而非仅在开头 prompt / 该动作自身描述里写。

## .codewiki/workspace 残留根因 + 清理
- 现象：Wiki 生成并 close_session 后，`.codewiki/workspace/` 仍残留 10 个文件。
- 根因：`.codewiki/workspace/` 是 MCP server 的大结果落盘 scratch 区（组件索引/源码 .src/sources/处理顺序）。`close_session` 只清理**当前活动会话 tracked** 的 scratch 文件；残留文件是 (1) 我直接用 write_to_file 写进该目录的辅助文件（module_tree_fixed.json/mod_comps.json/processing_order.json/component_list.json/prev_*/summary.json/schema.json/prompt_system_leaf.txt/doc_CLI_Adapter.md）—— 不走会话 tracking，close_session 无法感知；(2) 初始 analyze_repo 会话跨轮次残留。
- 关键事实：**BM25 搜索索引在 `.codewiki/analysis_cache.db`（与 workspace 分离）**，Wiki 正文在 repowiki/。故删 workspace/ 完全安全，不影响 query_wiki。已 `rm -rf .codewiki/workspace` 清理，复测 query_wiki 正常。
- 教训：Agent 生成过程中不应把临时辅助文件直接写进 MCP server 的 scratch 目录（`.codewiki/workspace/`），应在任务结束清理；或在别处（如 /tmp 或项目外）存放辅助文件。

## CodeWiki MCP 工作流端到端测试（25 方法遍历，报告在 .codewiki/mcp_workflow_test_report.md）
- 覆盖：25 个 MCP 方法中 24 个实测通过（generate_docs 为 legacy 黑盒需 LLM 配置跳过）；10 条工作流 prompt（catalog 7 条 + get_prompt 分析类 3 条）全部可取回。
- 发现的可修问题：
  1. `list_dependencies` 的 `module_level=true` 返回 total_modules=0（模块级聚合异常）。
  2. `view_repo_file` 路径基准为 `wiki/` 下（用 `repowiki/wiki/overview.md`，非 `repowiki/overview.md`）。
  3. `analyze_workspace` 单仓返回 "No git repositories found"：它扫子目录 `.git`（多仓语义），单仓无触发路径——设计限制。
  4. `retract_source` 的 remove_refs 软删，源文件移入 `repowiki/.trash/`，需额外清理。
  5. `edit_doc_file` 参数/路径与 `write_doc_file` 不一致：用 `filename`+`command`(str_replace/insert/undo)+`old_str/new_str`；只解析 `wiki/modules/`（不识别 `queries/`）；str_replace 唯一性同时计 frontmatter+正文。
  6. **`lint_wiki` 前后不一致 + 误报**：本会话早些时候报 0 issue/100，本次报 80 issue/0；2 个 error 是误报（MCP_Cache.md:56 的 `[text](x.md)` 为反引号内示例文字被当真实链接）；26 个 orphan/no_outlinks 因文档用裸 `[[ModuleName]]` wikilink 未被链接图解析；52 missing_aliases 因 frontmatter 无 aliases。可疑点：重跑 analyze_repo 重建缓存可能清空了 wiki 链接图，导致 orphan 判定失真。
- 测试产物已在 close_session 前清理；索引在 `.codewiki/analysis_cache.db`（与 workspace 分离），query_wiki 验证可用。

## 修复 CodeWiki MCP 代码缺陷（用户指令"修复代码"）
针对测试报告问题 5/6 落地代码修复，两文件 lint 通过 + 端到端验证：
**`codewiki/mcp/tools/wiki_lint.py`（问题 6）**
- 新增 `_strip_code_blocks()`：链接/wikilink 扫描前剥离围栏块与行内反引号，修复 `MCP_Cache.md:56` 反引号内 `[text](x.md)` 被误判断链。
- 新增 `_build_anchor_map()` + `_collect_linked_targets()`：orphan/no_outlinks 与 stale_refs 现解析裸 `[[Name]]`（按 title/slug/aliases 映射）与 `[[Name]](file.md)` wikilink。
- `_check_orphan_pages` / `_check_no_outlinks` 改用新逻辑 + anchor_map 复用。
- `_check_missing_aliases` 由逐文件 info 聚合为单条（count 字段），降低噪声与 health 影响。
- **验证**：repowiki 上 `lint_wiki` 从 80 issue/health 0 → 1 issue/health 99，断链误报 0，孤儿 26→0。

**`codewiki/mcp/tools/doc_writer.py`（问题 5）**
- 新增 `_split_frontmatter()`：str_replace 唯一性仅计 body，frontmatter 的 `description:` 回显正文句不再误报"出现 2 次"。同步修正 snippet 偏移计算基于 new_content 中 new_str 位置。
- 新增 `_find_doc_by_basename()`：edit 命中 page_type 路由失败时按 basename 在 `wiki/` 兜底定位，容忍 `page_type` 缺失/写错（不再报 File not found）。
- `_build_okf_frontmatter` / `_inject_lightweight_frontmatter` 默认注入 `aliases=[filename]`（除非显式提供），系统性消除 missing_aliases。
- **验证**：临时文档 + frontmatter 回显句 str_replace 成功；错误 page_type=query 仍能编辑 module 文档成功。

## flag_issue 移除冗余 session_id 参数（方案 B）
- 用户确认采用方案 B：删除 `flag_issue` 的隐藏 `session_id` 参数，统一用 `output_dir` 定位。
- 改动 `codewiki/mcp/tools/issue_tracker.py` `handle_flag_issue`：删除 `session_id` 读取与会话查找；`output_dir` 取 `arguments["output_dir"]`，省略时回退 `store.session_dir`（与其他工具 `resolve_session` 一致）；错误信息改为 "output_dir is required."；移除未使用的 `SessionState` 导入。
- 事实澄清：原 handler 中 `session_id` 仅用于反查 `session.output_dir`，且**不写入** issues.json 记录，纯属冗余；schema 的 `required` 本就只有 `issue_type`，session_id 连 properties 都没声明（隐藏参数）。
- **验证**：传 output_dir 成功；不传 output_dir 报 "output_dir is required."；传 session_id（现被忽略）+ output_dir 仍成功（向后兼容）。测试写入的 issues.json 已重置为空 tracker。

## MCP 重启后重测（2026-07-28 晚）
按用户"再测一遍mcp功能"+MCP开发指南，经真实 MCP server（已重启）重测：
- view_repo_file 存活；lint_wiki health=99（0 error/0 warn/1 info 聚合 missing_aliases）；flag_issue 无 session_id 成功；edit_doc_file str_replace+undo 成功且 git diff 空；query_wiki BM25 返回 5 篇+图谱；close_session 成功；close 后 lint 仍 health=99、query 仍可用。
- **清理遗留脏数据**：上一轮 edit 测试创建的 `wiki/modules/_tmp_edit_test.md` 被 unlink 但 `wiki/index.md` 第 35 行仍引用它 → 导致 2 个假断链 error，已从 index.md 删除该行（git diff 仅此行 + 头部时间戳变化）。
- 报告写入 `.codewiki/mcp_retest_report.md`。结论：三项代码修复经重启后真实调用全部生效、无回归，wiki health=99 可检索。

注：server.py 的 3 处 prompt 改动与本次两份文件改动均未提交 git（用户未要求）。

## CodeWiki MCP 全功能验证（25 工具逐一实测，隔离临时仓 .cw_verify，已清理）
用户要求「验证一遍 codewiki mcp 所有功能」。在工作区内建 `.cw_verify/`（单仓 + 双 git 子仓 service_a/client_b）隔离测试，25 个工具全部实测通过：
- **代码分析**：analyze_repo ✅(4 组件) / list_components ✅ / list_dependencies ✅ / analyze_impact ✅ / read_code_components ✅ / view_repo_file ✅
- **跨服务**：analyze_workspace ✅(2 仓/4 路由/2 链接) / query_cross_service ✅(http 链接置信度 1.0)
- **文档生成**：get_prompt(prompt_type) ✅ / save_module_tree(repo_path) ✅ / get_processing_order(repo_path) ✅ / write_doc_file(filename+page_type) ✅ / edit_doc_file(str_replace) ✅ / get_module_tree(legacy) ✅ / generate_docs(legacy) ⚠️
- **知识库**：query_wiki ✅(BM25 命中 doc/source/note) / ingest_note ✅ / confirm_note ✅ / reject_note ✅ / ingest_source ✅ / retract_source(dry_run) ✅ / batch_ingest ✅(源去重生效)
- **质量/会话**：lint_wiki ✅(8 issue/health 84) / flag_issue ✅(id fd7fd911) / close_session ✅

**关键发现（已核实：实为调用侧错误假设，非代码缺陷）**：
- 工具 JSON schema 即真理且被 server 强制校验：传错参数名会被直接拒绝（如 write_doc_file 缺 `filename` 报 required 错误）。真实字段：`write_doc_file`/`edit_doc_file` 用 `filename`+`page_type` 路由、`edit_doc_file` 用 `command`+`old_str/new_str`；`get_prompt` 用 `prompt_type`；`save_module_tree`/`get_processing_order`/`lint_wiki`/`flag_issue`/`query_wiki` 等用 `output_dir` 或 `repo_path`。
- 我最初误以为这些工具沿用 analyze 系列的 `session_id`/`relative_path`，属**调用侧假设错误**；AGENTS.md 实际只示例了 query_wiki/ingest_note（参数与真实 schema 一致）。**代码本身无需改**。
- 唯一可优化点在文档侧：AGENTS.md 未逐个列出各工具的必填参数，后续 agent 易误用 session_id/relative_path；如需可补一份「工具→必填参数」对照表。
2. `get_prompt` 用 `prompt_type` 枚举（cluster/system_leaf/...），非 `page_type`。
3. `save_module_tree` / `get_processing_order` 仅需 `repo_path`（推导 output_dir）。
4. **`generate_docs`(legacy) 实测报 400 `InvalidSubscription`**：它会调用火山引擎方舟(Volcengine Ark)远程 LLM 生成文档，当前账号(2123836311)无有效 CodingPlan 订阅。属环境/订阅缺失，非工具代码故障；需配置有效订阅才可用。
5. `analyze_workspace` 的 `total_components` 聚合显示为 0，但子仓 `list_components` 实际返回 2 个组件（数据存在，仅聚合展示瑕疵），跨服务链接(`query_cross_service`)完全正确。

## 修正：AGENTS.md 的 CodeWiki 段落实由代码生成（「改文档」=「改代码」）
用户纠正：AGENTS.md 里的 CodeWiki 段落实由 `codewiki/mcp/tools/agents_md.py` 的 `_build_section()` 在 `close_session` 时注入（HTML 注释定界 `<!-- CodeWiki LLM Wiki -->`/`<!-- /CodeWiki LLM Wiki -->`）。因此本次「补全工具参数文档」本质是**改代码**，非纯文档。已优化 `agents_md.py`：新增 `_TOOLS_QUICKREF` 常量与「MCP 工具参数速查」表（全部 25 工具的必填参数，并标注「`session_id` 仅被 analyze 返回、下游工具不接收，改用 `repo_path`/`output_dir`」陷阱），插入到生成段落后。已 python 校验 `_build_section` 正常输出、lint 无错。
**沉淀 lesson**：涉及「自动生成进 AGENTS.md/README 的文档」时，修正文档即修正生成它的代码模板，勿把二者割裂。要让仓库实际 AGENTS.md 更新，需改 `agents_md.py` 后重跑 `close_session` 或单独调用 `write_agents_md`（后者仅重写定界段，较安全）。

## 修复 analyze_workspace 的 total_components 恒为 0
**根因**：`codewiki/mcp/tools/workspace_analyzer.py:301` 取 `result.get("total_components", summary.get(...))`。但 `handle_analyze_repo` 的返回里 `total_components` 在 `stats` 子对象中（analysis.py:351，无顶层键），且兜底读的 `summary.json` 路径写成 `repo_output_dir/summary.json`，实际写在 `workspace.root`（由 `result["files"]["summary"]` 指示），路径对不上 → summary 为空 → 恒为 0。
**修复**：新增 `stats = result.get("stats") or {}`，`total_components/leaf_nodes/languages` 改从 `stats` 取（summary 兜底）；并把 summary 路径修正为 `result["files"]["summary"]`。
**验证**：绕过长驻 MCP server（其未热重载旧代码，故 MCP 调用仍显示 0），直接 `python3` 调用 `handle_analyze_workspace` —— total_components 由 0 变为 1/1，cross_service 正常。**注意：要让 IDE 内 MCP 调用生效，需重启 CodeWiki MCP server 进程。**
**操作 lesson**：改 codewiki MCP 代码后，端到端 MCP 调用可能仍跑旧进程；应直接 python 调用底层 handler 验证，或重启 MCP server。
