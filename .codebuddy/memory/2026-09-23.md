# 2026-09-23

## distill-worker 补蒸馏（任务「产品维护」）
- 处理 1 条 raw（conv-...-working-4，2026-09-21 会话），产出 2 条 draft 笔记：
  - `notes/2026-09-23-prompt-渲染正文不引用-adr设计文档行号源码注释保留-adr-指针.md`（decision）
  - `notes/2026-09-23-每日工作记忆文件是-append-only用-write-to-file-覆盖会丢当天早前记录.md`（pitfall）
- 教训（印证 Doctrine）：distill submit 带 dedup_action 重提交时**必须携带完整正文**，只带裁决说明会被判 invalid_note 丢弃；且 raw 归档后 task_id 过滤通道返回 noop，需用 raw_path 指向归档文件（不带 task_id）重试。
- 2 个弱匹配冲突候选（bm25 12-14）核实后判定全新知识，store 落盘。
- skill_hint：append-only 笔记命令密度 4，可能值得编译技能（待用户确认）。

## v5.13.0 发布完成（任务「发版本」）
- 版本判定：v5.12.0 后 5 个新提交含 1 feat（review_changes 委托模式 ADR-0016）→ minor bump 5.13.0。三处版本引用同步（pyproject.toml / codewiki/__init__.py / uv.lock:588）。
- 闸门：ruff 修复 32 错（22 自动 + 10 手工：F841×5、E741×5）+ 58 文件格式化；全量 pytest 1152 passed / 2 skipped。
- GitHub：bump commit 33dcca2 → PR #35 CI 绿（轮询 check-runs）→ 合并（merge 3e97d30）→ lightweight tag v5.13.0 → Release 已发布。
- **PyPI 关键坑**：本机网络对 upload.pypi.org 的 Python OpenSSL TLS 握手被干扰（uv publish / twine / requests 全超时），curl.exe（schannel 栈）正常。解法：Python 提取 whl METADATA / sdist PKG-INFO 构造完整表单 + curl.exe -K 配置文件直传 legacy API。digest 字段名是 `sha256_digest`（非 digests_sha256）。
- token 已出现在会话记录，建议轮换。

## 腾讯云 Harness 文章调研定案（任务「产品维护」）
- 用户裁决：三项借鉴全部不立项。①PreToolUse 硬拦截→absorbed（既有 P2-2 软闸门 + P2-1' 附加上下文已覆盖，claude-mem v13 源码反证 DENY 已被放弃）；②等待 skill→deferred（归「发版本」任务线）；③close-loop 闭环→excluded（依赖作者公司 CI/CD/工单系统，超产品边界）。
- 微借鉴一条：「不在中间任何一步静默停下」作为多步骤 prompt 写作原则。
- 调研笔记已落 draft：`notes/2026-09-23-腾讯云-harness-文章调研定案三项借鉴全部不立项去向-absorbeddeferredexcluded.md`，待 confirm。
- 本会话早前 2 条蒸馏草稿已被用户拒绝（deprecated）。
- 任务记忆 hot 层 65 条，压缩阈值已到，建议择机 compact_task_memories。

## Windows 闪窗修复（任务「产品维护」）
- 用户反馈：auto_push 自动提交 repowiki 时每次弹几个 cmd 窗口很快关闭。
- 根因：MCP server 由 IDE 无控制台拉起，git 子进程会新分配控制台窗口闪现；`git_sync.py` `run_git_bounded` 只设 `CREATE_NEW_PROCESS_GROUP` 没设 `CREATE_NO_WINDOW`。
- 修复：① `git_sync.py` 新增共享 helper `windows_creationflags()`（`CREATE_NO_WINDOW | CREATE_NEW_PROCESS_GROUP`，非 Windows 返回 0）；② 同款修复 MCP server 内 4 处直接 subprocess 调用（config.py / doc_writer.py / note_query.py / workspace_bootstrap.py）；③ 新增守门测试 `test_windows_creationflags_suppresses_console`。
- 验证：test_git_sync_auto_stage 12 passed + test_phase4_second_slice 11 passed。MCP server 需重启生效。

## subagent 指定模型调研（任务「产品维护」）
- 结论：CodeBuddy subagent frontmatter 支持 `model` 字段（可选，默认跟随主 Agent），官方示例 `model: glm-4.6`；distill-worker 当前未写 model，跑的是主 Agent 同款模型。
- 若要给蒸馏 worker 换弱模型：须改随包源变体 `codewiki/agents/distill-worker.md`（claude 家族变体取值不同需分别写）；因 toolsMCP「文档字段静默无效」教训，加字段后需真机验证生效。用户尚未决定是否加。
