"""CodeBuddy IDE hook wrappers shipped with codewiki.

This subpackage holds the *source* copies of hook scripts that the
``team-memory-hook`` MCP prompt copies into a user's project
(``<repo>/.codebuddy/hooks/``) when they enable team-memory capture.
The scripts are not invoked from inside this package — only the copied
files in the target project are run by the IDE.
"""
