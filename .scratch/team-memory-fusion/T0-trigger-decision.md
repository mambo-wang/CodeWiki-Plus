status: resolved
type: decision
labels: ready-for-agent
title: T0: capture_conversation 触发方式（IDE hook + 手动命令）
body: |
  > *This was generated by AI during triage.*

  ## Triage Notes

  **What we've established so far:**

  - MVP 已定：复用现有知识飞轮，新增 `capture_conversation`（T1）+ `distill_conversation`（T2）两个 MCP 工具。
  - 触发方式决策：**用户选择 both** —— 手动命令触发（主） + IDE hook 自动采集（可选）。

  **Decision (resolved):**
  - **手动命令触发**：用户/Session 结束时显式调用 `capture_conversation`（最简单，无 IDE 依赖）。属 T1 工具的自然使用方式，无需额外实现层。
  - **IDE hook 自动采集**：监听对话事件（消息落库/会话结束）自动调用 `capture_conversation`。需 IDE 侧接线，工程量更大，作为独立任务 **T6** 拆分，依赖 T1 完成。
  - T1–T5 不受影响，保持 `ready-for-agent` 并行开工。

  **Out of scope for T0:** IDE hook 的具体实现（见 T6）。

